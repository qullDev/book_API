basePath: /
host: localhost:8080
info:
  contact: {}
  description: RESTful service for managing books and categories with JWT authentication
  title: Book API
  version: "1.0"
paths: {}
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
